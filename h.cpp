#include<stdio.h>
int sum(int,int);
int main()
{
	int a,b,out;
	printf("enter the num");
	scanf("%d%d",&a,&b);
    out=sum(a,b);
    printf("%d",out);
    return 0;
}
int sum(int m,int x)
{
	if(m>x)
	for(i=1;i<=n;i++)
	{
	fact=fact*i;	
	}
	
}
